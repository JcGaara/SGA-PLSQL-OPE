DECLARE LN_COUNT NUMBER;
    
BEGIN
  LN_COUNT := 0;
  ------------------------------------------------------------------------------------------
  -- INSERCION DE PARAMETROS PARA GENERACION DE CONTRATO BSCS 
  ------------------------------------------------------------------------------------------
  ------------------------------------------------------------------------------------------
  -- INSERCION DE PARAMETRO PLAN
  ------------------------------------------------------------------------------------------ 
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='PLAN'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND CODIGON =200;
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, NULL,200,'Claro TV Sat','PLAN',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'));
    COMMIT;
  END IF;
  
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='PLAN'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND CODIGON =1496;
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, NULL,1496,'Plan 1 Play Internet','PLAN',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'));
    COMMIT;
  END IF;
  
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='PLAN'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND CODIGON =1498;
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, NULL,1498,'Plan 1 Play Cable','PLAN',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'));
    COMMIT;
  END IF;
  
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='PLAN'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND CODIGON =2323;
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, NULL,2323,'INTERNET INALAMBRICO 4MBPS','PLAN',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'));
    COMMIT;
  END IF;
  
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='PLAN'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND CODIGON =2326;
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, NULL,2326,'INTERNET INALAMBRICO 6MBPS','PLAN',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'));
    COMMIT;
  END IF;
  
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='PLAN'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND CODIGON =2327;
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, NULL,2327,'INTERNET INALAMBRICO 10MBPS','PLAN',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'));
    COMMIT;
  END IF;       
 ------------------------------------------------------------------------------------------
-- INSERCION DE PARAMETRO PLAN SISACT
------------------------------------------------------------------------------------------ 
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='PLAN_LTE_SISACT'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND CODIGON =1712;
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, NULL,1712,'PLAN LTE (IDSERV_PLAN)','PLAN_LTE_SISACT',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'));
    COMMIT;
  END IF;
     
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='PLAN_LTE_SISACT'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND CODIGON =5832;
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, NULL,5832,'PLAN LTE (IDSERV_PLAN)','PLAN_LTE_SISACT',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'));
    COMMIT;
  END IF;
    
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='PLAN_LTE_SISACT'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND CODIGON =5833;
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, NULL,5833,'PLAN LTE (IDSERV_PLAN)','PLAN_LTE_SISACT',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'));
    COMMIT;
  END IF;
    
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='PLAN_LTE_SISACT'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND CODIGON =5834;
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, NULL,5834,'PLAN LTE (IDSERV_PLAN)','PLAN_LTE_SISACT',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'));
    COMMIT;
  END IF;
    
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='PLAN_LTE_SISACT'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND CODIGON =6302;
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, NULL,6302,'PLAN LTE (IDSERV_PLAN)','PLAN_LTE_SISACT',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'));
    COMMIT;
  END IF;
    
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='PLAN_LTE_SISACT'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND CODIGON =6303;
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, NULL,6303,'PLAN LTE (IDSERV_PLAN)','PLAN_LTE_SISACT',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'));
    COMMIT;
  END IF;
    
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='PLAN_LTE_SISACT'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND CODIGON =6304;
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, NULL,6304,'PLAN LTE (IDSERV_PLAN)','PLAN_LTE_SISACT',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'));
    COMMIT;
  END IF;

  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='PLAN_LTE_SISACT'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND CODIGON =8487;
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, NULL,8487,'PLAN LTE (IDSERV_PLAN)','PLAN_LTE_SISACT',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'));
    COMMIT;
  END IF;
 
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='PLAN_LTE_SISACT'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND CODIGON =8488;
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, NULL,8488,'PLAN LTE (IDSERV_PLAN)','PLAN_LTE_SISACT',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'));
    COMMIT;
  END IF;
        
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='PLAN_LTE_SISACT'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND CODIGON =8504;
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, NULL,8504,'PLAN LTE (IDSERV_PLAN)','PLAN_LTE_SISACT',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'));
    COMMIT;
  END IF;
     
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='PLAN_LTE_SISACT'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND CODIGON =8507;
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, NULL,8507,'PLAN LTE (IDSERV_PLAN)','PLAN_LTE_SISACT',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'));
    COMMIT;
  END IF;
     
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='PLAN_LTE_SISACT'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND CODIGON =8899;
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, NULL,8899,'PLAN LTE (IDSERV_PLAN)','PLAN_LTE_SISACT',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'));
    COMMIT;
  END IF; 
 ------------------------------------------------------------------------------------------
-- INSERCION DE PARAMETRO SNCODE_DECO
-------------------------------------------------------------------------------------------
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='SNCODE_DECO'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND DESCRIPCION ='DECO_DVR 1';
  
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD
    ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD,CODIGON_AUX)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, 
         'DECO_DVR',
       2431,
       'DECO_DVR 1',
       'SNCODE_DECO',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'),
       1);         
    COMMIT;
  END IF;
  --
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='SNCODE_DECO'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND DESCRIPCION ='DECO_DVR 2';
  
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD
    ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD,CODIGON_AUX)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, 
         'DECO_DVR',
       2693,
       'DECO_DVR 2',
       'SNCODE_DECO',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'),
       1);         
    COMMIT;
  END IF;
  --
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='SNCODE_DECO'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND DESCRIPCION ='DECO_DVR 3';
  
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD
    ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD,CODIGON_AUX)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, 
         'DECO_DVR',
       2694,
       'DECO_DVR 3',
       'SNCODE_DECO',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'),
       1);         
    COMMIT;  
  END IF;
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='SNCODE_DECO'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND DESCRIPCION ='DECO_DVR 4';
  
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD
    ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD,CODIGON_AUX)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, 
         'DECO_DVR',
       2695,
       'DECO_DVR 4',
       'SNCODE_DECO',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'),
       1);         
    COMMIT;    
  END IF;    
  --
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='SNCODE_DECO'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND DESCRIPCION ='DECO_HD 1';
  
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD
    ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD,CODIGON_AUX)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, 
         'DECO_HD',
       3130,
       'DECO_HD 1',
       'SNCODE_DECO',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'),
       1);         
    COMMIT;    
  END IF;    
  --
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='SNCODE_DECO'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND DESCRIPCION ='DECO_HD 2';
  
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD
    ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD,CODIGON_AUX)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, 
         'DECO_HD',
       3131,
       'DECO_HD 2',
       'SNCODE_DECO',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'),
       1);         
    COMMIT;    
  END IF;    
--
  --
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='SNCODE_DECO'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND DESCRIPCION ='DECO_HD 3';
  
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD
    ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD,CODIGON_AUX)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, 
         'DECO_HD',
       3133,
       'DECO_HD 3',
       'SNCODE_DECO',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'),
       1);         
    COMMIT;    
  END IF;    
--
  --
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='SNCODE_DECO'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND DESCRIPCION ='DECO_HD 4';
  
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD
    ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD,CODIGON_AUX)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, 
         'DECO_HD',
       2699,
       'DECO_HD 4',
       'SNCODE_DECO',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'),
       1);         
    COMMIT;    
  END IF;    
--
  --
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='SNCODE_DECO'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND DESCRIPCION ='DECO_HD 5';
  
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD
    ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD,CODIGON_AUX)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, 
         'DECO_HD',
       2918,
       'DECO_HD 5',
       'SNCODE_DECO',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'),
       1);         
    COMMIT;   
  END IF;     
  -- SD
  --
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='SNCODE_DECO'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND DESCRIPCION ='DECO_SD 1';
  
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD
    ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD,CODIGON_AUX)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, 
         'DECO_SD',
       3785,
       'DECO_SD 1',
       'SNCODE_DECO',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'),
       1);         
    COMMIT;
  END IF;        
  --
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='SNCODE_DECO'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND DESCRIPCION ='DECO_SD 2';
  
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD
    ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD,CODIGON_AUX)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, 
         'DECO_SD',
       3128,
       'DECO_SD 2',
       'SNCODE_DECO',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'),
       1);         
    COMMIT;    
  END IF;    
--
  --
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='SNCODE_DECO'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND DESCRIPCION ='DECO_SD 3';
  
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD
    ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD,CODIGON_AUX)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, 
         'DECO_SD',
       3129,
       'DECO_SD 3',
       'SNCODE_DECO',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'),
       1);         
    COMMIT;    
  END IF;    
--
  --
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='SNCODE_DECO'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND DESCRIPCION ='DECO_SD 4';
  
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD
    ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD,CODIGON_AUX)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, 
         'DECO_SD',
       2690,
       'DECO_SD 4',
       'SNCODE_DECO',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'),
       1);         
    COMMIT;    
  END IF;    
--
  --
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='SNCODE_DECO'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO')
   AND DESCRIPCION ='DECO_SD 5';
  
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD
    ( IDOPEDD, CODIGOC, CODIGON, DESCRIPCION,ABREVIACION,TIPOPEDD,CODIGON_AUX)
    VALUES ( (SELECT MAX(IDOPEDD) FROM OPERACION.OPEDD)+1, 
         'DECO_SD',
       2831,
       'DECO_SD 5',
       'SNCODE_DECO',
             (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='BSCS_CONTRATO'),
       1);         
    COMMIT;    
  END IF; 
------------------------------------------------------------------------------------------
-- INSERCION DE PARAMETRO TRAMA_COLUMN
------------------------------------------------------------------------------------------ 
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='TRAMA_COLUMN'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='CONF_ALTA_PORTOUT')
   AND CODIGOC ='IDSRV_SISACT';
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD (IDOPEDD, CODIGOC, DESCRIPCION,ABREVIACION,TIPOPEDD) 
    VALUES ((SELECT MAX(IDOPEDD) + 1 FROM OPERACION.OPEDD), 'IDSRV_SISACT', 'SERVICIO','TRAMA_COLUMN',
           (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV = 'CONF_ALTA_PORTOUT'));
    COMMIT;
  END IF;  
  --
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='TRAMA_COLUMN'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='CONF_ALTA_PORTOUT')
   AND CODIGOC ='CANTIDAD';
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD (IDOPEDD, CODIGOC, DESCRIPCION,ABREVIACION,TIPOPEDD) 
    VALUES ((SELECT MAX(IDOPEDD) + 1 FROM OPERACION.OPEDD), 'CANTIDAD', 'CANTIDAD_INSTANCIA','TRAMA_COLUMN',
           (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV = 'CONF_ALTA_PORTOUT'));
    COMMIT;
  END IF;   
  --
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='TRAMA_COLUMN'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='CONF_ALTA_PORTOUT')
   AND CODIGOC ='CANTIDAD';
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD (IDOPEDD, CODIGOC, DESCRIPCION,ABREVIACION,TIPOPEDD) 
    VALUES ((SELECT MAX(IDOPEDD) + 1 FROM OPERACION.OPEDD), 'CANTIDAD', 'CANTIDAD_IDLINEA','TRAMA_COLUMN',
           (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV = 'CONF_ALTA_PORTOUT'));
    COMMIT;
  END IF;   
------------------------------------------------------------------------------------------
-- INSERCION DE PARAMETRO CONF_ALTA_PORTOUT -  GRUPO_PRINC
------------------------------------------------------------------------------------------   
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='GRUPO_PRINC'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='CONF_ALTA_PORTOUT');
   
   DELETE FROM OPERACION.OPEDD
   WHERE ABREVIACION ='GRUPO_PRINC'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='CONF_ALTA_PORTOUT');
   COMMIT;
  -- 
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='GRUPO_PRINC'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='CONF_ALTA_PORTOUT')
   AND CODIGOC ='201';
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD (IDOPEDD, CODIGOC, DESCRIPCION,ABREVIACION,CODIGON_AUX,TIPOPEDD) 
    VALUES ((SELECT MAX(IDOPEDD) + 1 FROM OPERACION.OPEDD), '201', 'LTE','GRUPO_PRINC',6,
           (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV = 'CONF_ALTA_PORTOUT'));
    COMMIT;
  END IF;  
  --
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='GRUPO_PRINC'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='CONF_ALTA_PORTOUT')
   AND CODIGOC ='203';
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD (IDOPEDD, CODIGOC, DESCRIPCION,ABREVIACION,CODIGON_AUX,TIPOPEDD) 
    VALUES ((SELECT MAX(IDOPEDD) + 1 FROM OPERACION.OPEDD), '203', 'LTE','GRUPO_PRINC',62,
           (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV = 'CONF_ALTA_PORTOUT'));
    COMMIT;
  END IF;  
  --  
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='GRUPO_PRINC'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='CONF_ALTA_PORTOUT')
   AND CODIGOC ='002';
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD (IDOPEDD, CODIGOC, DESCRIPCION,ABREVIACION,CODIGON_AUX,TIPOPEDD) 
    VALUES ((SELECT MAX(IDOPEDD) + 1 FROM OPERACION.OPEDD), '002', 'HFC','GRUPO_PRINC',6,
           (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV = 'CONF_ALTA_PORTOUT'));
    COMMIT;
  END IF;  
  --         
  SELECT COUNT(*)
    INTO LN_COUNT
    FROM OPERACION.OPEDD
   WHERE ABREVIACION ='GRUPO_PRINC'
   AND TIPOPEDD = (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV='CONF_ALTA_PORTOUT')
   AND CODIGOC ='003';
          
  IF LN_COUNT = 0 THEN  
    INSERT INTO OPERACION.OPEDD (IDOPEDD, CODIGOC, DESCRIPCION,ABREVIACION,CODIGON_AUX,TIPOPEDD) 
    VALUES ((SELECT MAX(IDOPEDD) + 1 FROM OPERACION.OPEDD), '003', 'HFC','GRUPO_PRINC',62,
           (SELECT TIPOPEDD FROM OPERACION.TIPOPEDD WHERE ABREV = 'CONF_ALTA_PORTOUT'));                
    COMMIT;
  END IF;      
END;
/